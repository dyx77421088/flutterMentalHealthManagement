import 'package:flutter/material.dart';

class WDXLoginPage extends StatelessWidget {
  static const String routeName = "/login";

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
